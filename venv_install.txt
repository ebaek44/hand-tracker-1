Venv Install Instructions

1. Clone Repo
git clone https://github.com/ebaek44/hand-tracker-1.git
cd your-repo

2. Create/activate Venv
python3 -m venv venv
source venv/bin/activate

3. Install all dependencies (depending on if you are on a mac vs other device)
pip install -e ".[apple]" or ".[cpu]"